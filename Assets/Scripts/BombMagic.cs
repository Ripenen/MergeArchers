using UnityEngine;

public class BombMagic : Magic
{
    public override void Run(PlayerTower tower, LevelTower target, Camera camera, GameFactory factory,
        GameBehaviour behaviour)
    {
    }
}